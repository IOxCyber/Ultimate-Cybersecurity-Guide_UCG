1. Cloudtrail:
- Use to log all the Data & Management related API  calls.
- Captures all API events in or out AWS resources i.e EC2, VPC network flow, All operations like accessing, deletion, creation of EC2 instances.

- Integrates with GuardDuty for threat detection.
- No need to deploy the Agents, captures via agentless.
