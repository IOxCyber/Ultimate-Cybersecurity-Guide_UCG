1. A host with QAgent needed.

### 3 Deployment Ways:
1. Install Qagent on endpoint hosts
2. Activate EDR module on hosts via `Qagent Tab`
3. Assign Host to a Configuration Profile with EDR enabled option `For Bulk`

## To Find Missing Assets with EDR:
1. EDR > Homepage > It will show Hosts > Click on Enable EDR > directs to Cloud Agent tab > All Assets without EDR > Select & Active via ACTION.

2. Enable EDR feature for Qagent in `Configuration Profile` > Open an Existing Profile in Qagent tab > Edit and Config accordingly to enable EDR.
