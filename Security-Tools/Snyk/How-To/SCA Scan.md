1. Snyk Test (To run SCA specific scan):
- Supported Fundings can be remediate from Snyk dashboard.
- Fix via `Open a Fix PR` > Review & Merge if all good.
- Usually the outdated version of dependencies, 3rd party libs.


2. Snyk Monitor (Uploads SCA findings only)
- Local findings identified via IDE project.
